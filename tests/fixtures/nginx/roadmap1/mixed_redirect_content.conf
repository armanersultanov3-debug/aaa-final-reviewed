server {
    listen 80;
    server_name mixed.example.test;

    location /old {
        return 301 https://mixed.example.test$request_uri;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
