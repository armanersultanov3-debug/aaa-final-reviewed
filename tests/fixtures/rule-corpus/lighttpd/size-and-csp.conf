server.port = 80
server.name = "example.test"
server.max-request-size = 200000
setenv.add-response-header = (
  "Content-Security-Policy" => "default-src * 'unsafe-inline' 'unsafe-eval'",
  "X-Content-Type-Options" => "nosniff"
)
